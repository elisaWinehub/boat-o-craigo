query CollectionsByHandle {
  wines: collectionByHandle(handle: "wines") {
    id
    handle
    title
    productsCount { count }
  }
  giftPacks: collectionByHandle(handle: "gift-packs") {
    id
    handle
    title
    productsCount { count }
  }
}
